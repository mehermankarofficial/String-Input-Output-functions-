#include<stdio.h>
#include<conio.h>
main()
{
char name[15];
clrscr();
puts("Enter your name :");
gets(name); // gets() is a function is used to receive a string from the keyboard
puts(name); // puts() is used to write a string to the screen
getch();
}
